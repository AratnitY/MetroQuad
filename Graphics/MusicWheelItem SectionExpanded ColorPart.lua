return Def.ActorFrame {

	Def.Quad {
		InitCommand=cmd(zoomto,350,42;diffuse,color("#FFFFFF");diffusealpha,0.3);
	};
	
	Def.Quad {
		InitCommand=cmd(zoomto,342,34;diffuse,color( LightBaseColor() );diffusealpha,0.8);
	};
	
};